[workspace]
resolver = "2"

members = [
  "flat_message",
  "flat_message_proc_macro",
  "benchmarks",
  "tests",
]


[profile.release]
debug = true
lto = "thin"

[workspace.lints.clippy]
module_inception = "allow"
new_without_default = "allow"
